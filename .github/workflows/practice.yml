name: lint, test and deploy
on: push
jobs:
  lint:
    runs-on: 
    steps:
      - name:
        run:
  test:
    needs: lint
    runs-on: 
    steps:
      - name:
        run:
  deploy:
    needs: test
    runs-on: 
    steps:
      - name:
        run:
