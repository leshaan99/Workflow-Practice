name: lint, test and deploy
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name:
        run:
  test:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - name:
        run:
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name:
        run:
